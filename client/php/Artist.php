<?php
require_once "../../DB.php";
class Artist
{
/*    static function addArtist($name_artist, $description_artist, $type_artist){ //TODO id type artist
        if(!empty($name_artist) and !empty($description_artist) and !empty($type_artist)){
            try {
                $db = DB::connexion();
                $name_artist = strval($name_artist);
                $description_artist = strval($description_artist);
                $type_artist



                $statement->execute();

                return "ok";
            } catch (PDOException $exception) {
                error_log('Request error: ' . $exception->getMessage());
                return "error";
            }
        }

    }*/
}